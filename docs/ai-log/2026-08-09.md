# AI 사용 기록 — 2026-08-09

## AI-017 — 팀원 브랜치 병합: 설계 충돌 조정, 보스 SLAM 낙사 버그 재수정

### 목적

`feat/director-policy-tuning` PR을 main에 병합하기 전, 팀원의 대규모 병합(패링·
아티팩트·상점·NPC·보스 전면 개편·방 1 튜토리얼 개편 등)과 실제로 겹치는지 확인하고
안전하게 합치기 위해서다.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
지금 팀원 브랜치가 메인에 머지 됐거든? 한번 내꺼 머지 되나 봐봐
```
(확인 후 충돌 상세 보고 → 사용자가 "응 그렇게 진행해줘"로 병합 진행 승인)

### AI 제안 요약

- `gh pr view`와 `git merge-tree`(비파괴)로 먼저 충돌 범위를 파악할 것을 제안 —
  실제로 머지하기 전에 무엇이 부딪히는지 알아야 안전하다고 판단.
- 텍스트 충돌 6개(Plan.md·WorkLog.md·ai-log·rooms.ts·Boss.ts·CombatScene.ts) 중
  코드 충돌 3개는 기계적으로 풀 수 있었지만, `rooms.ts`/`CombatScene.ts` diff를 먼저
  정독한 결과 팀원의 "방 1 무전투화"가 내 DEC-014(당시 번호, 현 DEC-016)의 설계
  전제(방 1 분석 → 방 2 반영) 자체를 무너뜨렸음을 발견해 사용자에게 먼저 보고.
  단순 충돌 해결로 넘어가지 않고 대안(방 2 1웨이브 텔레메트리로 대체 등 3가지)을
  제시해 사용자가 고르게 했다.
- 팀원의 "부활 루프"(사망해도 런 유지, 마을로 복귀)가 MVP_PLAN이 전제하던 permadeath
  로그라이크 구조 자체를 바꾼 것도 함께 보고 — 이미 사용자가 팀원과 확정한 변경(코드
  주석에 "사용자 확정"으로 명시)이라 되돌리자고 제안하지 않고 사실만 전달.

### 최종 반영

- 사용자가 "방 2 1웨이브 텔레메트리로 대체"를 선택. `RoomController.resolveWaveOverride`
  훅을 신설하고 `CombatScene`이 방 2 1웨이브 클리어 시점에 스타일을 분류해 2·3웨이브
  프리셋(`room_2_soft_*`)을 고르도록 구현. 기존 `resolveRoomId`는 제거.
- 병합된 main의 보스(팀원이 전면 개편한 버전)에도 어제와 같은 SLAM 낙사 버그가
  그대로 있음을 먼저 재현한 뒤, 같은 원인(`setAllowGravity(false)`가 `enemyBodies.add()`
  보다 먼저 호출됨)을 팀원의 새 스폰 코드(스프라이트 스케일·프레임 시스템 추가) 위에
  다시 적용했다. `git merge`가 이 3줄짜리 순서만 정확히 분리해 충돌 없이 병합해 줬다.
- DEC-013·DEC-014 번호가 main과 겹친 것을 발견해 내 결정만 DEC-015·DEC-016으로
  재번호(코드 주석·문서 전체, 팀원 것은 그대로 둠).
- 어제 만든 AnalysisPanel의 "방2/방3 예고 강약 차등화"가 방 1 무전투화로 분석
  시점이 하나로 줄면서 죽은 분기가 된 것을 확인하고 되돌렸다.

### 사람의 수정 및 판단

- 설계 충돌(게임 루프 변경급)을 발견했을 때 곧바로 고치지 않고 사용자에게 보고 후
  대안을 제시해 결정을 받았다 — CLAUDE.md의 "게임 루프 변경은 질문한다" 원칙.
- 완료 보고 전 실제 브라우저 검증 규칙에 따라, 헤드리스 자동화로 방 2 실전투(1웨이브
  → 2웨이브 전환)를 반복 시도했으나 팀원의 새 전투 난이도(웨이브당 4기, 부활 루프)에서
  자동화가 계속 사망해 완주하지 못했다. 이걸 감추지 않고, 대신 팀원이 dev용으로 이미
  노출해 둔 `window.phaserGame`/`window.runState` 훅으로 보스 씬에 직접 진입해
  `setBossWeights`로 SLAM을 100% 강제한 뒤 45초간 낙사 여부를 결정적으로 검증하는
  대체 경로를 스스로 찾아 실행했다. 못 한 것과 확인한 것을 구분해서 보고했다.
- 병합 과정에서 새로 발견한 문제(방 1의 빈 텔레메트리가 방 3 분석을 35%만큼 희석)를
  범위 밖이라 임의로 고치지 않고 OQ-031로 등록만 했다.

### 관련 파일

- `src/game/scenes/CombatScene.ts`, `src/game/systems/RoomController.ts`,
  `src/game/entities/Boss.ts`, `src/game/data/rooms.ts`, `src/game/data/directorRules.ts`,
  `src/components/ui/AnalysisPanel.tsx`, `src/components/HUDOverlay.tsx`
- `docs/DecisionLog.md`, `docs/OpenQuestions.md`, `docs/Plan.md`, `docs/WorkLog.md`,
  `docs/ai-log/2026-08-08.md`, `MVP_PLAN.md`

## AI-018 — OQ-031 수정: 방 1 무전투 텔레메트리가 방 3 분석을 희석하던 문제

### 목적

병합 과정에서 발견하고 OQ-031로만 등록해 뒀던 문제(방 1처럼 전투가 없던 방의 빈
텔레메트리가 `previousTelemetry`로 그대로 넘어가 다음 방 65/35 가중 분석을 희석함)를
실제로 고치기 위해서다.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
머지하고 발견한 문제도 수정하자 성향 판정 희석되는거
```

### AI 제안 요약

- `RunState.completeRoom()`이 방을 클리어할 때마다 무조건 `currentTelemetry`를
  `previousTelemetry`로 넘기던 것을, 그 방에 실제 신호(적중 또는 대시 1회 이상)가
  있었을 때만 넘기고 없으면 `null`로 두도록 수정.
- 구현 중 재검토하며, 이 희석이 근거리/원거리 "비율" 판정에는 수학적으로 영향이
  없고(분자·분모가 같은 배율로 줄어드는 구조라 비율 불변) 대시 횟수 절대값 기준인
  MOBILE 판정에만 실제로 영향을 준다는 점을 발견 — 처음에 문서에 적었던 "판정이
  전반적으로 약해진다"는 설명을 정정했다.

### 최종 반영

- `src/game/systems/RunState.ts`의 `completeRoom()`에 `previousHadSignal` 판정 추가.
- `window.runState` 개발용 훅으로 실제 전투 없이 방 클리어를 직접 재현해 검증:
  수정 전에는 방 2 클리어 후 `previousTelemetry`가 방 1의 빈 텔레메트리였고, 수정
  후에는 `null`(=방 2가 100% 가중치)임을 확인. 방 2→방 3처럼 둘 다 실신호가 있는
  정상 케이스는 그대로 65/35가 유지되는 회귀 없음도 함께 확인.
- `npm run typecheck`, `npm run lint`, `npm run build` 모두 통과.
- OQ-031을 RESOLVED로, `docs/WorkLog.md`에 검증 근거를 포함한 수정 기록을 추가.

### 사람의 수정 및 판단

- "판정이 희석된다"는 최초 가설을 구현 중 수학적으로 재검증해 스스로 정정했다 —
  실제로 영향받는 것은 MOBILE 대시 판정뿐이라는 점을 과장 없이 문서에 반영.
- 검증 스크립트(`verify-dilution-fix.mjs`)는 확인 후 삭제해 저장소에 남기지 않았다.

### 관련 파일

- `src/game/systems/RunState.ts`
- `docs/OpenQuestions.md`, `docs/WorkLog.md`, `docs/ai-log/2026-08-09.md`

## AI-019 — 화염·맹독 속성탄 킬 시 크래시 버그 분석·수정

### 목적

QA 중 사용자가 마주친 프로덕션 크래시(`Cannot read properties of null (reading 'x')`)의
원인을 찾기 위해서다. 콘솔 스택이 압축된 프로덕션 번들이라 파일·줄 번호로 바로 특정할
수 없었다.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
2번째 방으로 넘어가서 원거리무기로 바꾸고 한발 쏜 후에 다음 발 쏘려는데
적이 좀 커지면서 크래시났어 근데 맹독탄을 추가한 직후였어
```

### AI 제안 요약

- 스택만으로 위치를 못 좁혀 사용자에게 재현 상황을 되물었고, 답변("적이 커지는"
  히트펀치 연출·맹독탄 직후·다음 발 시도 시점)을 근거로 지연 데미지 틱(`delayedCall`)
  콜백 계열을 우선 의심했다.
- `CombatScene.applyElement`/`BossScene.applyElement`의 FIRE·POISON 틱 콜백이
  `takeDamage()` 호출 뒤에 `enemy.sprite.x`를 읽는 순서였고, `takeDamage()`가 그 틱으로
  적을 죽이면 `BaseEnemy.defeat()`가 콜백 도중 `sprite`를 null로 만든다는 것을 소스
  추적으로 확인. 같은 구조가 `BossScene.ts`에도 그대로 복제돼 있어 총 4곳을 특정했다.

### 최종 반영

- 4곳 모두 `takeDamage()` 호출 전에 `sprite.x`/`sprite.y`를 미리 캡처하도록 순서 변경.
- `npm run typecheck`, `npm run lint` 통과 확인.

### 사람의 수정 및 판단

- 재현 조건을 사용자에게 직접 물어 탐색 범위를 좁혔다(추측으로 코드베이스 전체를
  훑지 않음). 실브라우저 재현 확인은 사용자에게 넘겼다.

### 관련 파일

- `src/game/scenes/CombatScene.ts`, `src/game/scenes/BossScene.ts`
- `docs/WorkLog.md`, `docs/ai-log/2026-08-09.md`

---

## AI-018 — 저장소 전체 코드 리뷰와 지적 사항 일괄 수정

### 목적

빌드·린트·타입은 모두 통과하는 상태에서, 실행해야만 드러나는 기능 결함을 찾아내고
한 브랜치에서 정리하기 위해 사용했다.

### 사용 도구

- Claude Code (Opus 5) — 리뷰, 계약 확정, 통합 검증
- Claude Code 서브에이전트 (Sonnet) 3개 — 파일 소유권을 나눈 병렬 구현
- Codex CLI — 시도했으나 이 계정에서 지원 모델이 없어(400) 사용하지 못했다

### 대표 프롬프트

리뷰:

```
기능상으로 문제가 될만한 부분들을 찾아줘 꼼꼼하게
```

구현 위임(에이전트 C 예시, 요약):

```
수정 가능 파일: src/game/scenes/CombatScene.ts, BossScene.ts (이 2개만)
수정 금지: systems/**, types/**, data/**, entities/**, createGame.ts, components/**
사전 정보 — 다른 에이전트가 동시에 적용 중인 계약 변경:
  RoomPreset에서 hazardsEnabled 삭제, RoomControllerDeps에서 enableHazards 삭제
C-1. handleRoomClear에서 runState.hp 동기화 순서를 뒤집어 HEALTH_REGEN 회복이 덮이지 않게
C-2. pauseForPanel/resumeFromPanel을 두고 분석·강화·역기만 패널 동안 씬 정지
     (portalWipeOut은 트윈이라 화면 전환 전에 반드시 resume 먼저)
...
npm run build / lint / tsc 실행 금지 — 메인 에이전트가 한 번에 검증한다
```

### AI 제안 요약

- 6개 결함 렌즈(데이터 흐름·경계·오류 경로·계약·상태·보안)를 각각 따로 훑고,
  후보마다 실제 트리거 입력을 구성해 확인한 것만 보고하는 방식으로 12건을 확정했다.
- 판단이 필요한 두 갈래(함정 구현 여부, 디버그 치트 배포 정책)는 임의로 정하지 않고
  질문으로 올렸다.

### 최종 반영

DEC-017(함정 플래그 제거), DEC-018(F2를 `?debug=1`로 가둠)로 결정을 남기고,
지적 12건을 `fix/review-findings`에서 수정했다. 상세는 `docs/WorkLog.md` 2026-08-09 항목.

### 사람의 수정 및 판단

- **함정**: AI는 "구현 / 미구현 유지 / 플래그 제거" 세 갈래를 제시했고, 사람이 **플래그 제거**를
  택했다. 3일 차 이후 신규 핵심 기능을 만들지 않는다는 규칙에 맞다.
- **디버그 치트**: 완전 제거가 아니라 `?debug=1` 게이트를 택했다. 배포 URL로 시연할 길을
  남기면서 오조작만 막는 절충이다.
- **밸런스 사안 제외**: MIXED 소프트 카운터의 물량 비대칭(12기 vs 8기)은 AI가 발견했지만
  버그가 아니라 밸런스라 코드로 고치지 않고 OQ-033으로만 등록했다.
- **서브에이전트 보고를 그대로 신뢰하지 않았다.** 세 에이전트의 완료 보고를 받은 뒤
  실제 diff를 전부 읽어 범위 이탈·계약 위반이 없는지 확인하고 나서 검증에 들어갔다.

### 관련 파일

- `src/game/scenes/CombatScene.ts`, `src/game/scenes/BossScene.ts`
- `src/game/entities/Boss.ts`, `src/game/entities/enemies/BaseEnemy.ts`,
  `src/game/entities/enemies/MobilityCounterEnemy.ts`
- `src/game/systems/RoomController.ts`, `src/game/systems/RunState.ts`, `src/game/systems/audio.ts`
- `src/game/types/game.ts`, `src/game/data/rooms.ts`, `src/game/data/engravings.ts`
- `src/game/createGame.ts`, `src/components/ui/EngravePanel.tsx`
- `MVP_PLAN.md`, `docs/DecisionLog.md`, `docs/OpenQuestions.md`, `docs/Plan.md`, `docs/WorkLog.md`

---

## AI-020 — 방 1 서사 종료 직후 키맵 모달 추가 (P-038, DEC-020)

### 목적

사용자가 "스토리 이후에 조작키 안내 모달을 띄우자"고 요청한 것을 구현하기 위해서다.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
브랜치 새로 파서 처음에 스토리 이후에 튜토리얼 모달창? 같은거 띄우려고 해
키매핑 어떤 키에 어떤 기능이 들어가있는지 보여주는 그런거
```

### AI 제안 요약

- 구현 전에 `DialogueBox.tsx` 주석을 읽고, "서사에는 키 이름을 넣지 않는다·키 안내는
  설정 화면이 맡는다"는 기존 결정과 이번 요청이 정면으로 충돌함을 먼저 짚었다. OQ-026도
  그 지점에서 계속 `OPEN`이었다.
- 어느 쪽이 맞는지 임의로 정하지 않고 두 가지를 사용자에게 물었다: (1) 대화는 그대로 두고
  뒤에 별도 모달을 잇는가, 대화 자체를 키 안내형으로 바꾸는가, (2) 매번 보여줄지 최초
  1회만 보여줄지 — `DialogueBox`가 "재방문 시 건너뛰기" 로직에서 씬 자기 일시정지와
  경합해 영원히 멈추던 과거 버그를 참고 정보로 함께 제시했다.
- 사용자가 "대화 후 별도 모달·최초 1회"를 선택한 뒤에야 구현: `KeyMapModal.tsx` 신설,
  `HUDOverlay`에서 `DialogueBox.onDone` → (최초면) `KeyMapModal` → `game:resume` 순서로
  체인. 아티팩트로 나중에 풀리는 스킬 키(SKILL 계열)는 목록에서 제외했다.

### 최종 반영

- `src/components/ui/KeyMapModal.tsx`, `src/components/HUDOverlay.tsx` 수정.
- `docs/OpenQuestions.md` OQ-026 RESOLVED, `docs/DecisionLog.md` DEC-020 신설,
  `docs/Plan.md` P-038 추가·완료 처리.
- `npm run build`/`lint`/`typecheck` 통과. 기존 dev 서버(포트 3000, 팀원과 공유
  가능성이 있어 재시작하지 않음)를 그대로 이용해 `tmp/probe-keymap.mjs`(Playwright)로
  최초 진입 → 대화 → 모달 → 닫기 → 재개, 재방문 시 모달 미재현까지 실제 브라우저에서
  확인. 상세는 `docs/WorkLog.md` 참고.

### 사람의 수정 및 판단

- 서사와 키 안내를 분리한다는 기존 결정을 뒤집을지 여부를 AI가 스스로 정하지 않고
  사용자에게 확인받았다 — 결과적으로 "뒤집는 대신 보완"으로 정리됐다.
- 표시 빈도(매번 vs 최초 1회)도 과거 버그 이력을 근거로 제시한 뒤 사용자가 정했다.
- 새 브랜치를 처음에 잘못된 베이스(`fix/idle-zoom-between-waves`, main에 없는 커밋
  2개를 포함)에서 만든 것을 사람이 확인 전에 먼저 알아차리고 `origin/main` 기준으로
  다시 잡았다.

### 관련 파일

- `src/components/ui/KeyMapModal.tsx`, `src/components/HUDOverlay.tsx`
- `docs/OpenQuestions.md`, `docs/DecisionLog.md`, `docs/Plan.md`, `docs/WorkLog.md`

---

## AI-021 — 키맵 모달 피드백 3건 (ESC 안내 기본 노출·인벤토리 행 추가·H 도움말)

### 목적

방금 만든 키맵 모달을 사용자가 직접 확인한 뒤 준 피드백 3건을 반영하기 위해서다.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
일단 그 스토리 부분에 esc로 건너뛰기가 지금은 눌러야지만 나오는데 왼쪽 위에
기본적으로 떠있게 해주고, 키매핑에 인벤토리는 빠져있어. 그것도 추가해줘 그리고
언제든 다시 볼 수 있게 키매핑 보여주는 도움말? 키 하나 할당해서 나중에 언제든
띄울 수 있도록 해줘
```

### AI 제안 요약

- "인벤토리"라는 이름이 코드에 없어, 실제로 사용자가 말하는 것이 E로 여는
  `StatusPanel`(상태창)임을 먼저 확인했다. 이 E 키가 `KEY_BINDINGS`(설정에서 바꿀 수
  있는 전투 조작) 밖에 있는 `HUDOverlay` 하드코딩 토글이라 `KeyMapModal`의 `buildRows`가
  원천적으로 못 읽는 구조였음을 파악하고, 같은 방식으로 하드코딩 행을 추가했다.
- "언제든 다시 볼 키" 요청에는 새 키를 하나 골라야 했다. 기존 바인딩(A/D/SPACE/SHIFT/
  J/K/S/Q/R/F/W/ENTER/F1/F2/E/ESC)과 겹치지 않는 `H`(Help)를 코드베이스 전체 검색으로
  미사용 확인 후 채택.
- 최초 1회 자동 노출(`keymapOpen`)과 상시 재열람(`helpOpen`)을 하나의 상태로 합칠지
  분리할지 판단이 필요했다. 합치면 두 생애주기(1회성 vs 반복성)가 뒤섞여 나중에
  "왜 두 번째부터 markKeymapSeen이 또 불리지" 같은 혼란이 생길 수 있어 별도 상태로
  분리하고, 같은 `KeyMapModal` 컴포넌트만 재사용했다.

### 최종 반영

- `src/components/ui/DialogueBox.tsx` — `SkipGauge`의 `opacity: 0` 기본값 제거,
  `resetSkipGauge`가 더 이상 컨테이너를 숨기지 않고 링만 리셋.
- `src/components/ui/KeyMapModal.tsx` — "상태창(인벤토리)"(E), "도움말(이 화면)"(H) 행 추가.
- `src/components/HUDOverlay.tsx` — `helpOpen` 상태와 H 토글 이펙트 신설(E 상태창과
  같은 가드 조건, 열 때 `game:pause`/닫을 때 `game:resume`). ESC 일시정지·E 상태창
  이펙트의 가드에도 `helpOpen`을 추가해 세 오버레이가 서로 겹치지 않게 했다.
- `npm run build`/`lint`/`typecheck` 통과. 기존 dev 서버(포트 3000)로
  `tmp/probe-keymap2.mjs`(Playwright) 10항목 전부 통과.

### 사람의 수정 및 판단

- 세 가지 피드백 모두 구현 방향이 명확해 별도로 질문하지 않고 바로 처리했다 —
  이전 턴(DEC-020)에서 이미 "대화와 키 안내를 분리한다"는 설계가 확정돼 있어, 이번
  요청은 그 설계를 벗어나지 않는 보완이었다.
- H 키 선택은 사용자에게 묻지 않고 AI가 기존 바인딩과 충돌하지 않는 값을 스스로
  정했다 — 리바인딩 가능한 키가 아니라 E·ESC와 같은 하드코딩 토글이라 결과를 되돌리기
  쉽고, 관례적으로 Help에 쓰이는 글자라 낮은 리스크로 판단했다.

### 관련 파일

- `src/components/ui/DialogueBox.tsx`, `src/components/ui/KeyMapModal.tsx`,
  `src/components/HUDOverlay.tsx`
- `docs/Plan.md`, `docs/WorkLog.md`


## AI-022 — 미적용 에셋 추적과 인벤토리 프레임 적용 (P-039)

### 목적

팀원이 방금 추가한 에셋 중 실제로 코드에 꽂히지 않은 것이 있는지 찾고,
그중 하나인 `ui/inventory-frame.png`를 상태창에 적용.

### 사용 도구

Claude Code (Opus 5)

### 대표 프롬프트

- "지금 방금 팀원이 추가했던 에셋들 어디 적용된 데 있어?"
- "인벤토리 맞아 적용해놔"

### AI 제안 요약

- `public/assets` 211개 파일 각각을 `src` 전체에서 역참조해 미참조 93개를 뽑고,
  그중 장식 팩 여분(gate/pillar/arch 연번)을 제외하면 "쓰려고 넣었는데 안 꽂힌"
  파일은 `ui/inventory-frame.png` 하나임을 특정.
- 그림을 실제로 열어 보니 단순 테두리가 아니라 인벤토리 화면 통짜 목업이라,
  기존 제단 프레임처럼 `fill`로 깔 수 없다고 판단 — 바깥 장식 띠만 9-슬라이스로
  잘라 쓰는 안을 제시.
- slice 경계는 그림의 밝기 프로파일을 픽셀 스캔해 산출하고, `border-image-width`는
  CSS 9-슬라이스를 재현한 미리보기를 값별로 렌더링해 기존 제단 프레임과 비교해 선택.

### 최종 반영

`Panel`에 프레임별 slice·width·padding·maxWidth 노브를 추가(기본값은 기존 제단
프레임과 동일해 `EngravePanel` 무변경)하고, `StatusPanel`이 인벤토리 프레임 값을
넘기도록 했다. Playwright로 방 1 → 대화 스킵 → 키맵 모달 → E 상태창까지 실제로
관통해 적용·오버플로 0·콘솔 에러 0을 확인했다.

### 사람의 수정 및 판단

- "이 그림이 어느 패널용인가"는 AI가 정하지 않고 사용자에게 물었다 — 상점·강화
  패널일 가능성도 있었고, 대상이 정해져야 slice를 맞출 수 있었다.
- AI의 첫 결과물(기본 560px 폭)은 오른쪽 설명이 깨져 접혔다. 빌드 통과만으로
  넘기지 않고 실제 스크린샷을 보고 폭을 640px로 넓혔다.

### 관련 파일

- `src/components/ui/Panel.tsx`, `src/components/ui/StatusPanel.tsx`
- `docs/Plan.md`, `docs/WorkLog.md`


## AI-023 — 잡화 아티팩트 55종을 진짜 서로 다른 효과로 재설계 + 속성 태그 확장

### 목적

기존 55개 잡화 아티팩트가 12개 스탯 축을 공유해 4~5개씩 완전히 같은 효과였다.
사용자가 "효과 다 다르게"를 요청해 55종 전부를 고유한 기계적 효과로 다시 설계.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
효과 다 다르게 부탁해
(비용 경고 후) 진짜 60개 다 다르게
태그도 넣어줘 (암흑, 출혈 등등)
```

### AI 제안 요약

- 55개 전부 개별 발동 로직으로 다시 짜는 건 작업량이 크다고 판단해 착수 전에
  비용 경고를 먼저 전달하고 사용자 확인을 받았다.
- 9개 기계적 그룹(스탯 12축·적중 시 속성 확률 부여 14종·처치 시 효과·대시 연계·
  방 클리어 회복·주기 효과·패링/저체력·기타)으로 분류해 설계하고, `UpgradeElement`를
  3종(FIRE/FROST/POISON)에서 7종(+BLEED/DARK/SHOCK/HOLY)으로 확장했다.
- DEC-011(엔티티 직접 참조 금지) 제약 안에서 "적중 시 확률로 속성 부여"를 구현하려고
  공격 히트박스에 `itemProcs` 데이터 채널을 신설, 기존 스킬 히트박스가 쓰던 것과 같은
  `arena.playerAttacks` 충돌 파이프라인(`CombatScene.wireCollisions`) 안에서만 굴렸다 —
  스킬 강화가 쓰는 결정적 단일 `element` 필드와 달리 아이템은 매 히트마다 독립 확률로
  여러 속성이 동시에 발동할 수 있다.
- 방을 넘어가며 유지돼야 하는 효과(보호막, 근접 피해 버프)는 `Player`가 방마다
  `scene.restart()`로 다시 생성되는 특성상 인스턴스 필드로 못 남긴다 — 싱글턴
  `RunState`에 상태를 두고 `PlayerDeps` 콜백으로 노출하는 기존 `getShards` 패턴을 그대로 썼다.

### 최종 반영

`upgrades.ts`(55종 전면 재작성)·`types/game.ts`·`Player.ts`(proc 테이블·주기 효과·
`spawnProcHitbox` 신설)·`RunState.ts`·`CombatScene.ts`·`BossScene.ts`를 수정.
tsc·eslint·build 클린 확인 후, Chrome DevTools MCP로 48개 아이템을 동시 장착한
상태에서 근접·원거리·대시·킬·피격을 실제로 실행해 런타임 예외 0건을 확인하고
커밋·main 병합·배포까지 완료.

### 사람의 수정 및 판단

- 설계 자체는 그대로 승인됐다 — 별도 수정 지시 없이 다음 작업(상태창 재설계)으로 넘어갔다.
- 커밋 범위에서 무관한 에셋 덤프 폴더 2개와 우연히 섞인 lockfile diff는 사용자에게
  묻지 않고 AI가 스스로 제외해 커밋을 기능 단위로 좁게 유지했다.

### 관련 파일

- `src/game/data/upgrades.ts`, `src/game/types/game.ts`, `src/game/entities/Player.ts`
- `src/game/systems/RunState.ts`, `src/game/scenes/CombatScene.ts`, `src/game/scenes/BossScene.ts`


## AI-024 — 상태창(E) 전체 레이아웃 재현, HUD 아이콘 이미지화, 팀원 PR 충돌 해결

### 목적

사용자가 참고 이미지(인벤토리 목업 한 장)를 주고 "전체 레이아웃 재현"(탭 6칸·7x5
격자·원형 초상·게이지까지 이미지 그대로)을 선택 — 기존 CSS 시트형 상태창을 전면 교체.

### 사용 도구

Claude Code (Sonnet 5)

### 대표 프롬프트

```text
인벤토리 이미지는 이걸로 부탁 (이미지 첨부)
ui 변경작업 시작해
아이템 위치 안맞아. 그리고 탭부분 노란색 약간 안맞아
아니 비율이 아니라 중앙정렬이 아니야
오른쪽 사진 중앙 정렬 안맞아
너가 보고 충돌 잡아줘
```

### AI 제안 요약

- 이미지를 9-슬라이스 테두리가 아니라 통짜 배경으로 깔고, 탭·슬롯·초상화·게이지 좌표를
  실측(브라우저 렌더링을 크롭·정규화해 원본 1536×1024 좌표계로 역산, 밝기 프로파일
  스캔으로 격자선·탭 경계 픽셀 위치 검출)한 뒤 %로 환산한 오버레이 방식을 제안·구현.
- 초기 좌표는 눈대중이라 다섯 차례 어긋남(그리드 좌상단 66px 오프셋, 게이지 바가
  트로프 폭을 넘어 삐져나옴, 초상화 중심이 37px 아래로 잘못 측정됨 등)이 있었는데,
  매번 스크린샷을 정확한 위치에 크롭하고 크로스헤어를 찍어 재실측하는 방식으로 반복 수렴.
- 사용자가 준 8종 아이콘 시트(근접·원거리·기동·생존·재장전·검기·검극·검무)를 개별
  PNG로 분리하면서, 처음엔 알파 bbox로 타이트 크롭해 비대칭 이펙트(불꽃·궤적)가 한쪽에
  쏠려 보였다 — 사용자가 "비율이 아니라 정렬 문제"라고 정확히 짚어준 뒤에야 알파
  가중 중심(centroid) 기준 대칭 크롭으로 바꿔 해결.
- CSS에서 아이콘 `width`·`height`를 동시에 %로 강제해 세로로 긴 스킬 아이콘이 가로로
  눌려 있던 왜곡도 스스로 발견해 `max-width`/`max-height`로 교체.
- "배포 ㄱㄱ" 직후 `origin/main`을 당겨보니 팀원이 같은 파일(`StatusPanel.tsx`)을
  전혀 다른 설계(9-슬라이스 테두리만 쓰고 내용은 기존 CSS 유지, PR #6)로 이미 병합해둔
  상태를 발견 — 조용히 덮어쓰지 않고 두 안의 차이를 먼저 사용자에게 보고했다.
- 탭 아이콘(검·투구·물약·반지·좌대·주사위) 6개가 게임의 4개 카테고리(근접/원거리/
  기동/생존)에 완벽히 대응하지 않는다는 한계를 짚고, 물약=생존(회복 직관)으로 옮기고
  총/화살 그림이 없어 남는 투구가 원거리를 대신 맡는 절충안을 제시.

### 최종 반영

- `StatusPanel.tsx`를 `Panel` 컴포넌트 없이 자체 `Backdrop`+`Frame`으로 전면 재작성.
- `HudIcons.tsx` 8개 아이콘(근접/원거리/기동/생존/재장전/검기/검극/검무)을 인라인
  SVG에서 사용자 제공 이미지로 교체, `HUDOverlay.tsx`의 svg 전용 크기·애니메이션
  규칙을 `svg, img`로 확장.
- 탭 호버 시 밝기 강조, 탭 전환 시 그리드 페이드인 애니메이션 추가.
- 팀원 PR #6과의 병합 충돌은 `StatusPanel.tsx`만 우리 버전으로 취하고, 나머지 팀원
  변경(키맵 모달, 보스 스타일 기록, README/문서 갱신)은 그대로 살려 병합 — 병합된
  전체 트리로 tsc·eslint·build 재검증과 브라우저 스모크테스트까지 마친 뒤 배포.

### 사람의 수정 및 판단

- "전체 레이아웃 재현" vs "테두리만 교체"는 AI가 정하지 않고 먼저 물어 사용자가 결정.
- 아이콘이 중앙에 안 맞는 원인을 AI는 처음에 종횡비 왜곡으로 오판했다 — "비율이 아니라
  정렬 문제"라는 정확한 지적은 사용자가 직접 했고, 그제서야 알파 중심 크롭으로 고쳤다.
- 팀원 PR과의 충돌 처리 방향(내 버전으로 덮을지 보류할지)은 AI가 임의로 정하지 않고
  먼저 보고해 사용자가 "너가 보고 충돌 잡아줘"로 최종 승인했다.
- 아이콘 이미지 8종 자체는 AI가 생성한 게 아니라 사용자가 직접 제작해 제공했다 — AI는
  이미지 생성 도구가 없다는 점을 먼저 밝히고 대안을 물었다.

### 관련 파일

- `src/components/ui/StatusPanel.tsx`, `src/components/ui/HudIcons.tsx`
- `src/components/HUDOverlay.tsx`, `src/components/ui/Panel.tsx`
- `public/assets/ui/inventory-frame.png`, `public/assets/ui/skills/*.png`