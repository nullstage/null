/**
 * 그림자 조각 경제. 처치 보상과 마을 상인 가격을 한곳에서 관리한다. (DEC-014 #3)
 *
 * 조각은 `RunState.shards`에 쌓이고 부활해도 유지된다 — 죽어서 마을로 돌아온
 * 플레이어가 모아둔 조각으로 강화를 사서 재도전하는 순환이 목적이다.
 */
export const SHOP = {
  /** 적 하나가 떨어뜨리는 조각 수. */
  dropPerKill: { min: 1, max: 2 },
  /** 강화 하나의 가격. 방 하나(적 3~4)를 돌면 한 개쯤 살 수 있는 값이다. */
  price: 6,
  /** 상인이 한 번에 보여주는 강화 수. */
  choiceCount: 3,
  /** 마을에서 상인이 서 있는 x. 시작 지점과 게이트 사이, 걸어가며 자연스럽게 만난다. */
  merchantX: 640,
} as const;
