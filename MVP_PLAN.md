# Project NULL — 4일 MVP 개발 계획

> 핵심 목표는 스컬급 콘텐츠를 만드는 것이 아니라,  
> **플레이어를 분석하고 카운터하는 Director와 그 분석을 역이용하는 플레이어의 심리전**을  
> 5~8분짜리 한 번의 런에서 증명하는 것이다.

---

## 1. 최종 결과물

배포된 Web 빌드에서 다음 장면이 반드시 보여야 한다.

1. 원거리 위주로 플레이하면 다음 방에 추격형 적이 많이 등장한다.
2. 근거리 위주로 플레이하면 다음 방에 견제형 적이 많이 등장한다.
3. 플레이어가 분석 결과를 보고 전투 스타일을 바꾸면 Director의 예측이 실패한다.
4. 누적된 플레이 성향에 따라 보스가 사용하는 패턴 비중이 달라진다.
5. 전체 과정이 시작부터 결과 화면까지 5~8분 안에 끝난다.

핵심 루프:

```text
시작
→ 전투방 1
→ 분석
→ 강화 선택
→ 전투방 2
→ 분석 및 카운터 예고
→ 강화 선택
→ 카운터 전투방 3
→ 최종 분석
→ 적응형 보스
→ 결과 리포트
```

---

# 2. MVP 포함 범위

## 플레이어

캐릭터는 1명만 구현한다.

필수 기능:

- 좌우 이동
- 점프
- 대시
- 근거리 공격
- 원거리 공격
- 근거리·원거리 모드 전환
- 피격
- 사망
- 기본 타격 피드백

구현 기준:

- 근거리: 빠른 3연속 베기
- 원거리: 짧은 쿨타임의 투사체
- 모드 전환: 즉시 또는 최대 0.3초
- 대시: 짧은 무적 또는 충돌 회피
- 특수 공격과 패링은 제외

별도 무기 두 개가 아니라 **한 무기의 두 모드**로 구현한다.

## 적

적은 정확히 3종만 만든다.

### 추격형

- 플레이어에게 접근 후 근접 공격
- 원거리 플레이를 압박
- 돌진 전 예고 동작
- 근거리 공격에 경직되기 쉬움

### 견제형

- 일정 거리를 유지하며 투사체 발사
- 근거리 플레이를 견제
- 벽에 몰리면 취약
- 투사체는 점프나 대시로 회피 가능

### 기동 카운터형

- 플레이어의 이동 경로에 지연 폭발 장판 배치
- 장판은 즉시 폭발하지 않음
- 공격 범위를 사전에 표시
- 대시 자체가 아니라 반복적인 이동 습관을 카운터

## 방

기본 템플릿은 2개만 만든다.

- 가로형 방
- 발판형 방

실제 런 구성:

- 방 1: 조작과 전투 학습용 중립 방
- 방 2: 성향 분석용 혼합 방
- 방 3: Director가 선택한 카운터 방
- 보스방

Director가 변경하는 요소:

- 적 종류
- 적 수
- 스폰 순서
- 스폰 위치

장판은 방 단위 옵션이 아니라 기동 카운터형이 스스로 까는 것으로 정리했다. (DEC-017)

제외:

- 절차적 맵 생성
- 중력 변화
- 시야 제한
- 복잡한 지형 생성

## 로그라이크 요소

강화는 총 8개만 구현한다.

이 중 둘은 무기 자체를 바꾼다. 수치만 오르는 강화는 무엇이 좋아졌는지 화면에서 알 수 없어서,
이 둘은 궤적의 색과 굵기까지 함께 바뀌도록 한다. 고른 보람이 눈에 보여야 한다.

근거리:

- 근거리 공격력 20% 증가
- 근거리 마지막 타격 범위 증가
- **다시 벼린 검** — 피해 35%·사거리 증가, 궤적이 붉은색에서 흰빛으로 바뀐다

원거리:

- 원거리 공격 쿨타임 20% 감소
- 투사체가 적 1명을 관통
- **개조된 총열** — 피해 40%·탄속 증가, 탄 꼬리가 길고 두꺼워진다

기동:

- 대시 충전 +1
- 대시 직후 첫 공격 피해 증가

각 방 클리어 후 무작위 2개를 보여주고 하나를 선택한다.

제외:

- 등급
- 인벤토리
- 조합 효과
- 영구 성장
- 상점

---

# 3. 데이터 구조

## 전투 텔레메트리

방마다 다음 값만 기록한다.

```ts
interface CombatTelemetry {
  meleeAttacks: number;
  meleeHits: number;
  rangedAttacks: number;
  rangedHits: number;
  dashCount: number;
  airAttackCount: number;
  damageTakenCount: number;
  clearTimeMs: number;
  remainingHp: number;
}
```

MVP에서는 평균 교전 거리, 이동 경로 분석, 실시간 머신러닝을 구현하지 않는다.

## 플레이 스타일

```ts
type PlayStyle =
  | "MELEE"
  | "RANGED"
  | "MOBILE"
  | "MIXED";
```

## 분석 결과

```ts
interface DirectorAnalysis {
  style: PlayStyle;
  confidence: number;
  meleeRatio: number;
  rangedRatio: number;
  mobilityScore: number;
  counterRoomId: string;
  dialogueId: string;
}
```

---

# 4. 플레이 스타일 분류

## 기본 비율

```text
전체 적중 = 근거리 적중 + 원거리 적중
근거리 비율 = 근거리 적중 / 전체 적중
원거리 비율 = 원거리 적중 / 전체 적중
```

전체 적중이 0이면 `MIXED`로 처리한다.

## 분류 규칙

### RANGED

```text
원거리 비율 ≥ 0.60
```

### MELEE

```text
근거리 비율 ≥ 0.60
```

### MOBILE

다음 조건을 모두 만족할 때:

```text
근거리와 원거리 비율 차이 < 0.20
대시 횟수 ≥ 방 기준치
```

초기 대시 기준치는 방당 8회로 시작하고 플레이 테스트로 조정한다.

### MIXED

어느 조건에도 해당하지 않는 경우다.

## 최근 데이터 가중치

방 2 이후의 분석에는 최근 두 방을 사용한다.

```text
현재 방 65%
이전 방 35%
```

각 카운트에 가중치를 적용한 뒤 비율을 다시 계산한다.

예:

```text
weightedMeleeHits
= current.meleeHits × 0.65
+ previous.meleeHits × 0.35
```

## 신뢰도

MVP에서는 다음처럼 단순 계산한다.

```text
RANGED 또는 MELEE:
가장 높은 공격 비율 × 100

MOBILE:
대시 횟수 / 대시 기준치 × 70
단, 최대 95

MIXED:
50
```

신뢰도는 분석 UI를 위한 값이며 난이도를 직접 조절하지 않는다.

---

# 5. 카운터 방 정책

적 체력과 공격력은 성향에 따라 변하지 않는다.  
Director는 적 조합과 배치만 변경한다.

## RANGED 대응

```text
추격형 2
견제형 1
스폰 간격 좁게
```

의도:

- 거리를 유지하기 어렵게 함
- 근거리 모드로 전환하면 돌파하기 쉬움

## MELEE 대응

```text
견제형 2
추격형 1
좌우 분산 스폰
```

의도:

- 접근 동선을 복잡하게 함
- 원거리 모드로 전환하면 안정적으로 대응 가능

## MOBILE 대응

```text
기동 카운터형 2
추격형 1
```

장판은 기동 카운터형이 스스로 깐다. 방 단위 함정 플래그는 두지 않는다. (DEC-017)

의도:

- 반복 대시를 견제
- 장판 예고를 보고 이동 방향을 바꾸면 회피 가능

## MIXED 대응

```text
추격형 1
견제형 1
기동 카운터형 1
```

의도:

- 특정 스타일을 강제로 막지 않음
- 중립적인 전투 제공

## 방 2 소프트 카운터 (DEC-016)

방 2도 위 카운터 조합을 **축소판(2기)** 으로 반영한다. 각 카운터 조합에서 다수
그룹(2기)만 남기고 소수 그룹(1기)은 뺀다.

```text
RANGED 감지 → 추격형 2 (견제형 제외)
MELEE  감지 → 견제형 2 (추격형 제외)
MOBILE 감지 → 기동 카운터형 2 (추격형 제외, 지연 장판 유지)
MIXED  감지 → 기존 중립 방(방 2 원안) 그대로 유지
```

기준이 되는 스타일은 **방 2 1웨이브 텔레메트리만으로 분류한 결과**이고, 이 축소판은
방 2의 2·3웨이브에 적용된다. (원래는 "방 1 텔레메트리"였으나, 방 1이 무전투 튜토리얼로
바뀌면서 — 그리고 방 2 자체가 3웨이브 구조가 되면서 — 방 2 1웨이브를 관찰용으로 쓰는
방식으로 조정했다. DEC-016 "병합 후 갱신" 참고.)

의도:

- 방 3 하드 카운터보다 약한 규모로, 런당 "분석→카운터" 체감을 방 2·방 3 두 번으로 늘린다.
- 방 2(현재 웨이브)는 방 3 분석에서 65% 가중치를 차지하므로(§4), 2·3웨이브를 특정
  스타일로 과도하게 몰면 방 2 1웨이브 결과가 방 2 자신의 최종 텔레메트리를 거쳐 방 3
  분석에 되먹임되는 위험이 있다. 다수 그룹만 남기고 소수 그룹은 완전히 빼는 대신,
  방 3 하드 카운터(3기)보다 적은 2기로 제한해 그 위험을 낮춘다.

---

# 6. 역기만 판정

별도 위장 스킬은 구현하지 않는다.

플레이어가 실제 플레이 방식을 바꾸는 것이 역기만이다.

## 판정 시점

카운터 방 3을 클리어한 직후 판정한다.

## 카운터 방의 실제 스타일

방 3에서 수집한 텔레메트리를 같은 분류 규칙으로 다시 계산한다.

## 성공 조건

```text
Director가 방 3 입장 전에 예측한 스타일
!=
방 3에서 실제로 사용한 대표 스타일

그리고

방 3 클리어 성공
```

`MIXED`로 바뀐 경우도 예측 실패로 인정한다.

## 성공 보상

1차 구현은 체력 20% 회복으로 고정한다.

```text
회복량 = 최대 체력 × 0.20
최대 체력을 넘지 않음
```

화면 표시:

```text
「판결」

기록이 어긋났다

적어 둔 방식: 원거리
실제로 택한 방식: 근거리
되돌려받은 것: 생명 20

“기록이 어긋났다. 다시 본다.”
```

의도적 기만인지 자동으로 추론하지 않는다.  
예측이 빗나갔고 플레이어가 방을 통과했다면 성공으로 처리한다.

---

# 7. 분석 UI

## 문구 톤

Director는 시스템이나 AI가 아니라 **NULL의 기록자이자 심판**이다.
NULL은 돌아오지 못한 자들의 시험장이고, 그는 시험받는 자를 지켜보고 적어 둔다.

- 시작 화면 프롤로그: "시험받는 자는 셀 수 없이 많았으나, 돌아온 자의 이름을 아는 이는 없었다."
- 화면 표기는 「기록」 / 「판결」 / 「남은 기록」 / 「주어진 것」 계열 한국어를 쓴다.
- `DIRECTOR ANALYSIS`, `PREDICTION FAILED` 같은 영문 헤더는 쓰지 않는다.
- 대사는 담담한 고어체 단문이다. 감탄사, 이모지, 과장, 위협을 쓰지 않는다.
- 파란색 계열을 쓰지 않는다. 다크 판타지(붉은 달, 폐허) 톤을 따른다.

이 톤은 표기와 대사에만 적용된다. 분류 규칙, 임계값, 가중치는 영향받지 않는다.
코드의 식별자(`DirectorPolicy`, `DirectorAnalysis` 등)는 그대로 둔다.

## 방 클리어 후 팝업

필수 표시:

- 감지된 플레이 스타일
- 근거리 또는 원거리 비율
- 대시 의존도
- 분석 신뢰도
- Director 대사

예:

```text
「기록」

택한 방식: 원거리
가까이서 맞힌 비율: 18%
멀리서 맞힌 비율: 72%
물러선 정도: 높음
기록의 확신: 81%

“가까이 오는 법을 잊었군.”
“다음 방에는 네가 설 거리가 남지 않을 것이다.”
```

## 대시 의존도

```text
낮음: 0~3회
보통: 4~7회
높음: 8회 이상
```

## 디버그 UI

`F1`로 표시한다.

```text
현재 방
근거리 적중
원거리 적중
대시 횟수
현재 분류
신뢰도
선택한 카운터 방
보스 패턴 가중치
```

디버그 UI는 제출 영상의 증거 장면에도 사용할 수 있지만, 일반 플레이에서는 꺼진 상태가 기본이다.

---

# 8. 보스 설계

보스는 1마리만 만든다.

## 기본 패턴

- 근거리 베기
- 돌진
- 원거리 투사체
- 점프 내려찍기

```ts
interface BossPatternWeights {
  slash: number;
  dash: number;
  projectile: number;
  slam: number;
}
```

## 기본 가중치

```text
slash: 25
dash: 25
projectile: 25
slam: 25
```

## RANGED 플레이어

```text
slash: 15
dash: 40
projectile: 10
slam: 35
```

## MELEE 플레이어

```text
slash: 25
dash: 10
projectile: 45
slam: 20
```

## MOBILE 플레이어

```text
slash: 15
dash: 20
projectile: 20
slam: 45
```

점프 내려찍기 후 지연 장판을 남기는 효과는 시간이 남을 때만 추가한다.

## MIXED 플레이어

기본 가중치를 사용한다.

## 패턴 선택 규칙

- 보스전 시작 전에 최종 성향을 확정한다.
- 전투 중 재분석하지 않는다.
- 동일 패턴이 3회 연속 선택되지 않도록 제한한다.
- 패턴 선택 전 최소 대기시간을 둔다.
- 보스 체력, 공격력, 공격 속도는 성향에 따라 변경하지 않는다.

---

# 9. 게임 상태 흐름

```text
BOOT
→ READY
→ COMBAT_ROOM_1
→ ANALYSIS
→ UPGRADE
→ COMBAT_ROOM_2
→ ANALYSIS
→ UPGRADE
→ COUNTER_ROOM
→ DECEPTION_RESULT
→ BOSS
→ RESULT
```

사망 시:

```text
GAME_OVER
→ RESTART
→ 모든 RunState 초기화
→ READY 또는 COMBAT_ROOM_1
```

반드시 초기화할 값:

- 현재 방 번호
- 현재 방 텔레메트리
- 이전 방 텔레메트리
- 누적 플레이 스타일
- 선택한 강화
- Director 예측
- 카운터 방 ID
- 보스 패턴 가중치
- 기만 성공 여부

---

# 10. 배포 구조

```text
GitHub Repository
→ GitHub Actions
→ npm run build
→ dist/
→ GitHub Pages
```

## 구현 원칙

- 핵심 게임 로직은 전부 브라우저에서 실행한다.
- 외부 API와 데이터베이스에 의존하지 않는다.
- 런 데이터는 메모리에 유지한다.
- 새로고침 시 런이 초기화되어도 괜찮다.
- 필요하면 설정값만 `localStorage`에 저장한다.
- 첫날 배포 URL을 만든다.

## 배포 완료 조건

- 비로그인 상태에서 URL 접속 가능
- 설치 없이 시작 가능
- 에셋과 사운드 정상 로딩
- 새로고침 후 빈 화면 없음
- 1366×768과 1920×1080에서 UI가 잘리지 않음

---

# 11. 권장 파일 구조

스택은 Next.js + Phaser 3 + GSAP + Emotion이다(DEC-005).
UI는 Phaser 내부가 아니라 캔버스 위 React DOM 오버레이에 둔다(DEC-006).

```text
app/                        Next.js App Router 셸
├─ layout.tsx
├─ page.tsx
└─ globals.css

src/
├─ components/              React UI 레이어 (Emotion + GSAP)
│  ├─ GameShell.tsx         캔버스 + 오버레이 합성
│  ├─ GameCanvas.tsx        Phaser 마운트 (ssr:false)
│  ├─ EmotionRegistry.tsx   정적 프리렌더용 스타일 주입
│  ├─ HUDOverlay.tsx        이벤트 → 패널 전환의 단일 진입점
│  └─ ui/
│     ├─ Panel.tsx          팝업 공통 껍데기와 등장 연출
│     ├─ AnalysisPanel.tsx
│     ├─ UpgradePanel.tsx
│     ├─ DeceptionPanel.tsx
│     ├─ ResultPanel.tsx
│     └─ DebugPanel.tsx     F1
├─ hooks/
│  └─ useGameEvent.ts
├─ styles/
│  └─ theme.ts
└─ game/                    Phaser 런타임
   ├─ createGame.ts
   ├─ EventBus.ts           Phaser ↔ React 유일한 통신 경로
   ├─ scenes/
   │  ├─ BootScene.ts
   │  ├─ ReadyScene.ts
   │  ├─ CombatScene.ts     방 1~3
   │  └─ BossScene.ts
   ├─ entities/
   │  ├─ Player.ts
   │  ├─ Boss.ts
   │  └─ enemies/
   │     ├─ BaseEnemy.ts
   │     ├─ ChaserEnemy.ts
   │     ├─ RangedEnemy.ts
   │     └─ MobilityCounterEnemy.ts
   ├─ systems/
   │  ├─ CombatTelemetry.ts
   │  ├─ DirectorPolicy.ts
   │  ├─ RoomController.ts
   │  ├─ UpgradeSystem.ts
   │  └─ RunState.ts
   ├─ data/
   │  ├─ enemies.ts
   │  ├─ rooms.ts
   │  ├─ upgrades.ts
   │  └─ directorRules.ts
   ├─ config/
   │  ├─ gameConfig.ts
   │  ├─ gameBalance.ts
   │  └─ inputConfig.ts
   └─ types/
      └─ game.ts

public/                     정적 에셋
docs/
├─ ai-log/
└─ WorkLog.md
```

결과 화면은 React 패널이므로 별도 `ResultScene`을 만들지 않는다.

실제 프로젝트 구조가 이미 있다면 이를 억지로 맞추지 말고 책임만 동일하게 분리한다.

---

# 12. 4일 개발 일정

## Day 1 — 플레이 가능한 기본 전투와 최초 배포

### 담당 A: 전투

- 이동
- 점프
- 대시
- 근거리 공격
- 원거리 공격
- 모드 전환
- 플레이어 피격과 사망
- 더미 적 피격과 사망
- 기본 히트스톱 또는 카메라 흔들림

### 담당 B: 시스템과 배포

- 게임 상태 구조
- 방 시작과 종료
- `CombatTelemetry` 타입
- 공격, 적중, 대시, 피격 기록
- 임시 분류 함수
- GitHub Actions
- GitHub Pages 최초 배포

### Day 1 완료 조건

- 배포 URL 접속 가능
- 캐릭터 이동, 점프, 대시 가능
- 근거리와 원거리 공격 가능
- 더미 적 처치 가능
- 방 클리어 이벤트 발생
- 콘솔에서 텔레메트리 확인 가능
- 근거리와 원거리 플레이 분류 가능

### Day 1 실패 시 삭제

- 공격 콤보 애니메이션 축소
- 카메라 효과 축소
- 별도 준비 공간 제외

---

## Day 2 — 적 3종과 Counter Director

### 담당 A: 적과 방

- 추격형 적
- 견제형 적
- 기동 카운터형 적
- 공격 예고
- 스포너
- 가로형 방
- 발판형 방
- 방 1, 방 2, 방 3 연결

### 담당 B: Director

- 최근 두 방 가중치 계산
- MELEE, RANGED, MOBILE, MIXED 분류
- 신뢰도 계산
- 카운터 방 프리셋
- 분석 팝업
- Director 대사
- F1 디버그 UI

### Day 2 완료 조건

- 원거리 위주 플레이 시 추격형 중심 방 생성
- 근거리 위주 플레이 시 견제형 중심 방 생성
- 대시 위주 플레이 시 기동 카운터 방 생성
- 분석 결과와 실제 방 구성이 일치
- 배포 환경에서도 동일하게 작동

### Day 2 실패 시 삭제

- 기동형 분류와 적을 마지막 우선순위로 내림
- 발판형 방을 제외하고 가로형 방 하나만 재사용
- Director 대사를 1개씩으로 축소

---

## Day 3 — 강화, 역기만, 보스, 전체 런

### 담당 A: 보스

- 보스 이동과 상태 전환
- 베기
- 돌진
- 투사체
- 점프 내려찍기
- 보스 체력 UI
- 패턴 가중치 적용
- 사망 처리

### 담당 B: 런 시스템

- 강화 8종
- 강화 선택 UI
- 방 3 실제 플레이 스타일 재분석
- 예측 실패 판정
- 체력 20% 회복
- 결과 리포트
- 재시작과 상태 초기화

### Day 3 완료 조건

- 시작부터 보스 클리어까지 진행 가능
- 원거리와 근거리 플레이에서 보스 행동이 다름
- 스타일 전환 시 예측 실패 표시
- 사망 후 처음부터 재시작 가능
- 전체 런 5~8분

### Day 3 종료 규칙

이 시점 이후 신규 핵심 기능을 추가하지 않는다.

### Day 3 실패 시 삭제

- 강화 8개를 4개로 축소
- 기만 성공 보상을 메시지만 표시
- 보스 패턴을 4개에서 3개로 축소
- 별도 Result Scene 대신 팝업 사용

---

## Day 4 — 버그 수정, 밸런스, 제출

### 오전: 안정화

- 충돌 오류
- 적이 벽에 끼는 문제
- 적이 남았는데 방이 종료되는 문제
- 적이 없는데 문이 열리지 않는 문제
- 방 클리어 이벤트 중복
- 강화 선택 중 중복 입력
- 보스 상태 전환 오류
- 재시작 후 이전 데이터 잔존
- GitHub Pages 에셋 경로 오류

### 오후: 밸런스

목표 수치:

- 일반방: 45~70초
- 전체 런: 5~8분
- 일반 적: 기본 공격 3~5회로 처치
- 플레이어: 실수 4~6회 허용
- 보스전: 90~120초
- 소프트 카운터: 불편하지만 스타일 전환이나 숙련으로 극복 가능

### 저녁: 시연 촬영

#### 플레이 A

```text
원거리 위주
→ RANGED 분석
→ 추격형 중심 카운터 방
→ 보스 돌진 비중 증가
```

#### 플레이 B

```text
근거리 위주
→ MELEE 분석
→ 견제형 중심 카운터 방
→ 보스 투사체 비중 증가
```

#### 플레이 C

```text
초반 원거리 위주
→ RANGED 분석
→ 근거리 강화 선택
→ 방 3에서 근거리 전환
→ PREDICTION FAILED
```

### 최종 검증

- 시크릿 창에서 접속
- GitHub 로그아웃 상태에서 접속
- 캐시 삭제 후 접속
- 1366×768 테스트
- 1920×1080 테스트
- 전체 런 3회 연속 완료
- 브라우저 콘솔 치명적 오류 없음
- 배포 URL 고정
- AI 사용 로그 정리

---

# 13. 2인 팀 역할

## 전투 담당

- 플레이어 컨트롤
- 공격과 피격
- 적 3종
- 방 템플릿
- 보스
- 사운드와 타격 피드백

## 시스템 담당

- 게임 상태
- 전투 텔레메트리
- Director 정책
- 카운터 방 선택
- 강화
- 분석 UI
- 결과 리포트
- GitHub Pages 배포
- AI 로그와 시연 자료

## 통합 시간

매일 최소 두 번 통합한다.

```text
14:00 1차 통합
21:00 최종 통합
```

통합 전 각자 다음을 확인한다.

- 담당 브랜치 빌드 성공
- 치명적 콘솔 오류 없음
- 수정 파일 목록 공유
- 상대 담당 파일을 불필요하게 수정하지 않음

---

# 14. 삭제 우선순위

일정이 밀리면 아래 순서대로 삭제한다.

1. 준비 공간
2. Director 애니메이션
3. 강화 8개 중 2개
4. 기만 성공 체력 보상
5. 발판형 방
6. 기동형 분류
7. 기동 카운터형 적
8. 보스 점프 내려찍기

다음은 삭제하면 안 된다.

1. 근거리와 원거리 공격
2. 전투 텔레메트리
3. MELEE와 RANGED 분류
4. 서로 다른 카운터 방
5. 분석 결과 UI
6. 보스 패턴 가중치 변화
7. 처음부터 결과까지 이어지는 한 번의 런

---

# 15. MVP 성공 판정

다음 항목이 모두 충족되면 성공이다.

- [ ] 배포 URL에서 즉시 실행된다.
- [ ] 기본 전투가 답답하지 않다.
- [ ] 원거리와 근거리 플레이에서 방 3 구성이 명확히 달라진다.
- [ ] 플레이어가 분석 결과와 다음 방의 관계를 이해할 수 있다.
- [ ] 스타일을 바꿔 Director의 예측을 실패시킬 수 있다.
- [ ] 보스 행동 비중이 플레이 성향에 따라 달라진다.
- [ ] 사망과 재시작이 정상 동작한다.
- [ ] 한 런이 5~8분 안에 끝난다.
- [ ] 디버그 UI 없이도 핵심 시스템이 보인다.
- [ ] 동일한 빌드에서 플레이 A, B, C를 재현할 수 있다.
