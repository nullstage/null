# Decision Log

> 확정된 주요 기술·정책·범위 결정과 근거를 기록한다. 단순 구현 세부사항은 기록하지 않는다.

## DEC-001 — 프로젝트 핵심 경험

- 날짜: 2026-08-06
- 상태: ACCEPTED

### 결정

게임의 핵심은 플레이어 전투 분석, 다음 방 카운터 구성, 플레이 스타일 전환을 통한 역기만, 보스 패턴 변화로 고정한다.

### 이유

4일 안에 구현 가능한 범위에서 Counter Director가 단순 난이도 조절이 아니라 플레이어와 수 싸움을 벌이는 시스템임을 명확히 보여주기 위해서다.

### 영향

- 방 3은 분석 결과에 따라 적 구성이 달라져야 한다.
- 보스는 성향별 패턴 가중치를 사용해야 한다.
- 분석 결과와 예측 실패가 UI에 보여야 한다.

---

## DEC-002 — 클라이언트 중심 정적 배포

- 날짜: 2026-08-06
- 상태: ACCEPTED

### 결정

게임은 GitHub Pages에 Web 빌드로 배포하고, 전투 분석과 Director 판단은 클라이언트에서 수행한다. 별도 백엔드와 데이터베이스는 사용하지 않는다.

### 이유

심사 URL 접근성과 배포 안정성을 확보하고, 4일 MVP에서 서버 개발·운영 리스크를 제거하기 위해서다.

### 대안

- 별도 API 서버와 장기 프로필 저장: 기간과 안정성 문제로 제외
- 실시간 LLM 호출: 네트워크 실패와 응답 지연 때문에 제외

---

## DEC-003 — 규칙 기반 Counter Director

- 날짜: 2026-08-06
- 상태: ACCEPTED

### 결정

플레이 스타일은 근거리, 원거리, 기동, 혼합형으로 분류하며 규칙 기반 정책 엔진으로 카운터 방과 보스 패턴 가중치를 결정한다.

### 이유

설명 가능하고 재현 가능하며, 플레이어가 자신의 행동과 결과의 관계를 이해할 수 있기 때문이다.

### 대안

- 머신러닝 분류 모델: 학습 데이터와 검증 시간이 없어 제외
- 생성형 AI 판단: 비결정성과 외부 API 의존 때문에 제외

---

## DEC-004 — 소프트 카운터

- 날짜: 2026-08-06
- 상태: ACCEPTED

### 결정

Director는 플레이어의 주력 수단을 무효화하지 않고, 스타일 전환이나 숙련으로 극복할 수 있는 적 조합과 패턴을 사용한다.

### 이유

하드 카운터는 분석당했다는 재미보다 불공정하다는 인상을 줄 수 있기 때문이다.

---

## DEC-005 — 기술 스택과 배포 형태

- 날짜: 2026-08-06
- 상태: ACCEPTED
- 관련 질문: OQ-001, OQ-002

### 결정

다음 스택으로 확정한다.

```text
Next.js (App Router) — 셸과 UI 레이어
Phaser 3 — 게임 런타임
GSAP — UI 연출
Emotion — UI 스타일
TypeScript — 전 영역
```

배포는 Next.js 정적 내보내기(`output: "export"`)로 생성한 정적 파일을 GitHub Pages에 올린다.
저장소는 `nullstage/null`을 사용하고 `basePath`는 `/null`로 설정한다.

### 이유

- 팀원이 사용할 스택이 Phaser·Next.js·GSAP·Emotion으로 정해져 있다.
- 정적 내보내기를 쓰면 서버 없이 GitHub Pages에 그대로 올릴 수 있어 DEC-002와 충돌하지 않는다.
- Phaser 메이저 버전은 3으로 고정한다. `MVP_PLAN.md`와 `CLAUDE.md`가 Phaser 3을 전제로 작성되어 있고, 팀원이 참고할 전투·보스 구현 패턴 자료도 3 기준이 압도적으로 많다.

### 대안

- Vite + Phaser 단독: 더 가볍지만 팀원 스택과 어긋나고 Emotion·GSAP UI 레이어를 살리기 어렵다.
- Next.js 서버 렌더링 사용: GitHub Pages가 정적 호스팅이라 불가능하다.
- Phaser 4: 최신이지만 자료가 적고 MVP_PLAN 전제와 어긋난다. 필요하면 별도 결정으로 올린다.

### 영향

- Phaser는 `window`에 의존하므로 정적 프리렌더 중 실행되면 안 된다. 캔버스 컴포넌트는 `ssr: false` 동적 임포트로만 마운트한다.
- 정적 경로 문제 때문에 에셋 경로는 `basePath`를 반영해야 한다.
- `MVP_PLAN.md` §11 파일 구조를 Next.js 구조에 맞게 갱신한다.

---

## DEC-006 — UI를 React DOM 오버레이로 분리

- 날짜: 2026-08-06
- 상태: ACCEPTED
- 관련 질문: OQ-001

### 결정

분석 팝업, 강화 선택, 결과 리포트, 디버그 패널은 Phaser 내부가 아니라 캔버스 위에 얹는 React 컴포넌트로 구현한다.
Phaser와 React는 단일 이벤트 버스로만 통신하고 서로의 객체를 직접 참조하지 않는다.

```text
Phaser  --emit-->  EventBus  --subscribe-->  React UI
React   --emit-->  EventBus  --subscribe-->  Phaser
```

### 이유

- Emotion과 GSAP을 스택에 넣은 목적이 UI 스타일과 연출이므로 UI가 DOM에 있어야 의미가 있다.
- 팀원은 Phaser 씬과 전투에 집중하고, 시스템 담당은 React UI에 집중할 수 있어 파일 충돌이 줄어든다.
- 텍스트 레이아웃, 반응형, 접근성 처리가 Phaser Text보다 쉽다.

### 대안

- Phaser 내부 UI: MVP_PLAN 원안이지만 Emotion·GSAP이 사실상 사용되지 않는다.
- 혼합: 전투 HUD만 Phaser 내부. 유효한 절충안이라 전투 HUD는 이후 필요 시 Phaser로 옮길 수 있다.

### 영향

- `MVP_PLAN.md` §11의 `src/game/ui/*.ts`는 `src/components/ui/*.tsx`로 대체된다.
- 게임 로직은 React 상태에 의존하지 않아야 한다. UI가 없어도 런은 성립해야 한다.

---

## DEC-007 — 작업 책임 분리

- 날짜: 2026-08-06
- 상태: ACCEPTED

### 결정

| 영역 | 담당 | 범위 |
|---|---|---|
| 전투 | 팀원 | 플레이어 조작·공격·피격, 적 3종 동작, 보스 상태와 패턴 실행, 타격 피드백 |
| 시스템 | 본인 | 게임 상태, 텔레메트리, Director 정책, 강화, React UI, 이벤트 버스, 배포 |

현재 단계에서는 시스템 영역만 동작하도록 구현하고, 전투 영역은 타입과 호출 시점이 확정된 빈 스텁으로 남긴다.

### 이유

전투 감각은 직접 만드는 사람이 잡아야 하고, 미리 구현해두면 팀원이 버리는 코드가 된다.
반대로 계약과 호출 시점이 없으면 나중에 통합할 때 서로의 파일을 고쳐야 한다.

### 영향

- 스텁 파일에는 구현 대신 `// 팀원 담당` 주석과 시그니처만 둔다.
- 텔레메트리 기록 호출 지점은 시스템 담당이 미리 심어두고, 팀원은 그 자리를 지운다.
- 공통 타입(`src/game/types/game.ts`)은 시스템 담당이 관리하며 임의로 변경하지 않는다.

---

## DEC-008 — 시작 화면은 Figma 디자인을 React 레이어로 구현한다

- 날짜: 2026-08-06
- 상태: ACCEPTED
- 관련 질문: OQ-024, OQ-026

### 결정

시작 화면(Figma `Frame 1`, node `1:33`)을 Phaser 씬이 아니라 React 컴포넌트
`src/components/ui/TitleScreen.tsx`로 그린다. `ReadyScene`은 화면을 그리지 않고
`ui:continue` 이벤트만 기다린다. 시작 입력(키·클릭)도 React가 단독으로 받는다.

에셋은 `public/assets/title/`에 두고 `assetPath()`로 참조한다.
배치는 1920×1080 디자인 좌표를 그대로 쓰고, `--u`(디자인 1px의 실제 px) 하나로 스케일한다.

### 이유

- 배경 블러·웹폰트·투명 PNG 겹치기는 DOM이 Phaser보다 훨씬 짧게 끝난다.
- DEC-006에서 이미 "UI는 React DOM 오버레이"로 경계를 그었다. 시작 화면은 UI다.
- 입력 지점을 한 곳으로 모아야 런이 두 번 시작되지 않는다.

### 대안

- Phaser `ReadyScene`에서 이미지로 렌더 — 블러와 한글 웹폰트 처리 비용이 크고,
  캔버스 내부 좌표계에 묶여 1920×1080 디자인 좌표를 그대로 못 쓴다.
- Next.js `next/image` 정적 임포트 — `basePath` 처리는 편하지만 기존 `assetPath()`
  규약과 이중이 되고, `next-env.d.ts`가 없으면 `npm run typecheck`가 깨진다.

### 영향

- `src/components/ui/TitleScreen.tsx` 신규, `HUDOverlay`가 `phase === "READY"`에 렌더한다.
- `ReadyScene`에서 플레이스홀더 텍스트와 자체 입력 처리를 제거했다. 조작 안내가 사라져 OQ-026을 등록했다.
- `RunState.reset()`이 `phase` 필드를 직접 대입해 `phase:change`를 발행하지 않던 문제를 함께 고쳤다.
  이 이벤트가 없으면 React가 READY 화면을 띄우지 못한다.
- `theme.font.ui`(Pretendard)와 `app/globals.css`의 웹폰트 `@import`가 추가됐다.

---

## DEC-009 — 화면 연출은 GSAP, 전환은 ScreenFade 한 곳에서

- 날짜: 2026-08-06
- 상태: ACCEPTED
- 관련 질문: OQ-027

### 결정

- 시작 화면 등장과 로딩 화면 연출은 GSAP 타임라인으로 만든다. 이미 의존성에 있고 순서 제어가 쉽다.
- 화면 사이 전환은 `src/components/ui/ScreenFade.tsx` 하나만 쓴다.
  검게 덮은 시점(`onCovered`)에 다음 화면으로 넘기고 다시 걷는다.
- 시작 화면 → 전투 전환은 `none → cover → load → none` 3단계다.
  덮인 뒤 로딩 화면을 한 번 더 보여 주고 전투로 들어간다.
  그래서 `theme.z.loading`(60)이 `theme.z.transition`(50)보다 위다.
- 로딩 화면은 시작 화면 에셋 네 장을 미리 받고, **걷히기 시작할 때** 다음 화면을 먼저 붙인다.
  다 걷힌 뒤에 붙이면 그 사이 빈 화면이 보인다.
- 시작 화면 배경은 16:9 무대 밖에 두고 화면 전체를 덮는다(`object-fit: cover`).
  무대 안에 두면 16:9가 아닌 창에서 빈 띠가 남는다. 배경은 잘려도 되고 타이틀은 잘리면 안 된다.
- 모든 연출은 `gsap.matchMedia()`로 `prefers-reduced-motion`을 존중한다.

### 이유

- 전환 길이가 화면마다 달라지면 게임 전체의 리듬이 흐트러진다. 한 컴포넌트로 묶어 고정한다.
- 배경 이미지가 1MB를 넘어 프리로드 없이는 시작 화면에서 배경이 뒤늦게 튀어나온다.

### 대안

- CSS `@keyframes` — 요소별 순서를 맞추기 어렵고, 길이를 조정할 때 손댈 곳이 흩어진다.
- 전환 없이 즉시 교체 — 구현은 가장 짧지만 화면이 툭툭 끊긴다.
- 배경까지 `contain` — Figma 구도는 정확히 보존되지만 창 비율이 다르면 빈 띠가 남는다.

### 영향

- `LoadingScreen.tsx`, `ScreenFade.tsx`, `titleAssets.ts` 신규. `TitleScreen.tsx`에 등장 타임라인 추가.
- `HUDOverlay`가 로딩·전환 상태를 함께 관리한다.
- `theme.z`에 `loading: 40`, `transition: 50` 추가.
- `ornament.png`는 Figma 렌더 방향(상하 반전)으로 파일 자체를 뒤집었다.
  CSS `transform`으로 뒤집으면 GSAP이 같은 속성을 덮어써 충돌한다.
- 로딩 화면 디자인은 Figma 시안이 없어 임시안이다. OQ-027로 남겼다.

---

## DEC-010 — 시작 화면 메뉴와 소리 진입 방식

- 날짜: 2026-08-06
- 상태: ACCEPTED
- 관련 질문: OQ-027, OQ-028

### 결정

- 시작 화면은 "아무 키" 대신 **처음부터 / 이어하기 / 설정** 메뉴로 진입한다.
  서체는 Pretendard Light 흰색, 항목 배경은 가운데만 진한 가로 그라데이션이다.
- **이어하기는 비활성**이다. 세이브·로드는 MVP 범위 밖이라 UI만 두고 기능은 넣지 않는다. (OQ-028)
- 설정 화면은 **왼쪽 세로 아이콘 레일 + 오른쪽 행 목록** 구성이다(사운드 / 조작).
  소리는 마스터·배경음악·효과음 세 값을 둔다. 마스터는 나머지에 곱해서 쓴다.
  효과음은 아직 메뉴 소리에만 걸리므로 화면에 그 범위를 적는다.
  값은 `localStorage` 키 `null:audio` 하나에 JSON으로 저장한다.
- 배경음악은 로딩 화면에서 시작해 전투 전환과 함께 끈다. 메인 화면 전용이다.
- **첫 방문에는 안내 팝업을 먼저 띄운다.** 브라우저가 사용자 입력 전 소리를 막으므로
  이 버튼 클릭이 소리를 여는 입력 역할을 한다. 방문 여부만 `localStorage`에 남긴다.
- 마우스 커서는 mobicom 프로젝트 구현을 가져와 색만 바꿔 쓴다.
- 브라우저 기본 포커스 링은 지우지 않고 색만 붉은 톤으로 바꾼다.

### 이유

- 메뉴가 있어야 설정 진입점이 생기고, 심사자가 조작 방법을 짐작하지 않아도 된다.
- 자동 재생 차단은 우회할 수 없다. 안내 팝업이 규칙(CLAUDE.md 배포 규칙)과 요구를 동시에 만족시킨다.
- 포커스 링을 없애면 키보드 사용자가 위치를 잃는다. 색만 바꾸면 톤과 접근성을 모두 지킨다.

### 대안

- 자동 재생 시도 후 실패하면 무음 — 첫 방문자는 소리를 못 듣는다.
- 이어하기를 메뉴에서 제거 — 나중에 넣을 때 메뉴 배치를 다시 잡아야 한다.

### 영향

- `TitleBgm.tsx`, `SettingsPanel.tsx`, `FirstVisitPrompt.tsx`, `CursorFollower.tsx` 신규.
- `TitleScreen`이 메뉴와 설정 상태를 갖고, `HUDOverlay`가 볼륨·첫 방문 상태를 관리한다.
- `localStorage` 키 두 개를 쓴다: `null:visited`, `null:bgmVolume`.
- `theme.z`에 `prompt: 70`, `cursor: 9999` 추가.

---

## DEC-011 — 디렉터를 「기록자」로 재해석하고 전투 계약을 분리한다

- 날짜: 2026-08-06
- 상태: ACCEPTED
- 관련 질문: OQ-021 (RESOLVED), OQ-024

### 결정

**톤.** 기획서의 "AI 디렉터"(실험·분석하는 SF 존재)를 지금 만든 다크 판타지 화면에 맞춰
**기록하고 판결하는 존재**로 재해석한다. NULL은 돌아오지 못한 자들의 시험장이다.
- 화면 표기는 「기록」/「판결」 계열 한국어. `DIRECTOR ANALYSIS` 같은 영문 헤더는 쓰지 않는다.
- 영문은 `NOTICE`·`SETTINGS` 같은 소제목에만 남긴다.
- **코드 식별자(`DirectorPolicy`, `DirectorAnalysis` 등)는 바꾸지 않는다.** 표시 문구만 교체한다.

**전투 계약.** 플레이어·적·보스를 나눠서 만들 수 있도록 `src/game/types/combat.ts`에
충돌 그룹(`CombatArena`)과 데이터 규약을 정의한다.
- 엔티티는 서로를 직접 참조하지 않는다. 각자 자기 그룹에만 오브젝트를 넣는다.
- 누가 누구를 때렸는지는 **씬 한 곳에서만** 판정한다.
- 플레이어 위치는 `getPlayerPosition()`으로만 얻는다.
- 도형 팔레트는 `SILHOUETTE`/`TEXTURE` 한 곳에 모은다. 스프라이트가 오면 키만 교체한다. (OQ-024)

### 이유

- 시스템(분석 → 카운터 → 역기만 → 보스 가중치)은 그대로 두고 표현만 바꾸면 리팩터 비용이 0이다.
  식별자까지 바꾸면 검증해 둔 분류 규칙을 다시 확인해야 한다.
- 계약이 없으면 여러 담당이 같은 씬 파일을 동시에 고쳐 충돌한다. 실제로 이 계약 덕분에
  플레이어·적·보스·톤을 네 갈래로 나눠 병렬 작업할 수 있었다.

### 대안

- SF 톤 유지 — 이미 만든 아트(붉은 달·폐허)와 어긋나 아트를 다시 잡아야 한다.
- 엔티티가 서로를 직접 참조 — 구현은 빠르지만 파일 소유권을 나눌 수 없다.

### 영향

- `data/directorRules.ts`의 대사, `AnalysisPanel`·`DeceptionPanel`·`ResultPanel` 문구, `MVP_PLAN.md` 서술.
  임계값·가중치 숫자는 하나도 바뀌지 않았다.
- `types/combat.ts` 신규. `CombatScene`·`BossScene`이 충돌을 배선한다.
- 전투 수치는 아직 각 엔티티 파일 상단에 임시로 모여 있다. 밸런스 확정 후 `gameBalance.ts`로 옮긴다. (OQ-007)

## DEC-012 — 강화를 8종으로 늘리고 둘은 무기 자체를 바꾼다

- 날짜: 2026-08-07
- 상태: 확정

### 결정

강화를 6종에서 **8종**으로 늘린다. 추가한 둘은 무기 자체를 교체하는 강화다.

- `BLADE_REFORGED` (다시 벼린 검) — 피해 1.35배, 사거리 +14, 궤적이 붉은색에서 흰빛으로
- `BARREL_REFORGED` (개조된 총열) — 피해 1.4배, 탄속 1.25배, 탄 꼬리가 84 → 124로 길고 두껍게

### 근거

기존 6종은 전부 숫자만 바뀐다. 「예리한 날」을 골라도 화면에서는 아무것도 달라지지 않아
무엇을 얻었는지 플레이어가 알 수 없었다. 강화를 고르는 순간이 런의 리듬을 만드는 지점인데
그 보람이 보이지 않는 것은 손해다.

그래서 이 둘은 **수치와 이펙트를 같이 바꾼다.** 벼린 검은 궤적 색이 바뀌고 획이 굵어지며,
개조된 총열은 탄 꼬리가 눈에 띄게 길어진다. 다음 방에 들어서는 순간 손에 든 것이
달라졌음을 한 번의 공격으로 알 수 있다.

### 검토한 대안

- **6종 유지, 이펙트만 기존 강화에 붙이기** — 「예리한 날」이 색까지 바꾸면
  이름과 효과가 어긋난다. 날을 갈았는데 빛이 바뀔 이유가 없다.
- **무기 교체를 별도 시스템으로** — 인벤토리·장착 UI가 따라온다. MVP 범위 밖이다. (CLAUDE.md 규칙 3)

### 영향 범위

- `types/game.ts` — `UpgradeId`에 두 값 추가. 기존 값은 그대로라 다른 코드는 영향 없다.
- `data/upgrades.ts` — 정의 2개 추가. `UPGRADE_IDS`가 자동으로 8개가 되어 선택지에 등장한다.
- `systems/CombatVfx.ts` — `slashArc`·`createBulletTrail`에 기본값 있는 선택 인자 추가.
- `entities/Player.ts` — 수치 적용과 VFX 전달.
- `MVP_PLAN.md` — 강화 6종 → 8종으로 갱신.

적 체력과 공격력은 건드리지 않았다. 소프트 카운터 원칙은 그대로다. (DEC-004)

강화 지급 횟수는 여전히 2회다(OQ-016 미결정). 후보가 6종에서 8종으로 늘어난 만큼
특정 강화를 만날 확률은 낮아진다. 지급 횟수를 늘릴지는 플레이 테스트 후 결정한다.

## DEC-013 — 방 1(튜토리얼)을 무전투 + 게이트 진행 방으로 바꾼다

- 날짜: 2026-08-07
- 상태: 확정 (사용자 명시 요청)

### 결정

방 1의 적 스폰을 전부 제거한다(`spawns: []`). 진행은 적 처치가 아니라 방 끝에 세운
**전송 게이트**를 밟는 것으로 한다. 게이트를 밟으면 분석(ANALYSIS) 단계를 건너뛰고
바로 강화 선택(UPGRADE)으로 넘어간다.

### 이유

- 사용자 명시 요청: "튜토리얼 스테이지에는 적 없게하고. 포탈로 스테이지 넘어가게."
- 전투 데이터가 없는 방을 Director가 분석하면 전부 0으로 나와 의미가 없다. 화면에
  "MIXED, 신뢰도 0%" 같은 빈 분석 팝업을 띄우는 것보다는 건너뛰는 게 낫다.
- 강화 지급 횟수(런당 2회, OQ-016)는 그대로 유지한다 — 분석만 건너뛰고 강화 제안은
  그대로 두어, 방 1을 비워도 플레이어가 얻는 보상 기회 수는 줄지 않는다.

### 검토한 대안

- **적 없는 방도 그대로 분석 팝업을 띄운다** — 빈 데이터로 분석 UI를 보여주는 건
  오히려 혼란을 준다. 기각.
- **`RoomController.forceClear()`를 게이트 트리거로 재사용** — 적이 0이면
  `RoomController.start()`가 방 시작과 동시에 이미 `finish()`를 호출해버려서(빈 프리셋
  소프트락 방지 안전장치), 게이트를 밟았을 때 다시 불러도 이미 `cleared=true`라 아무 일도
  안 일어난다. 대신 `handleRoomClear`가 그 즉시 발생하는 클리어 호출을 가로채 콜백만
  보관해두고(`awaitPortal`), 실제 게이트 오버랩 때 그 콜백을 직접 실행하는 방식으로 우회했다.

### 영향 범위

- `data/rooms.ts` — room_1 `spawns: []`.
- `scenes/CombatScene.ts` — `handleRoomClear`에 방 1 분기(`awaitPortal`) 추가, 게이트
  스프라이트 생성 + 오버랩 배선, 배경/바닥 타일을 방 1 전용으로 분리.
- `types/combat.ts` — `addDecor` 추가, 배경 렌더링을 "좁은 방은 크롭, 넓은 방은 통짜
  확대"로 변경(반복 타일링 제거 — 사용자가 "무한스크롤 하지 말고 사진을 키워서" 요청).
- 개발용 `DEBUG_SKIP_ROOM` 키는 방 1에서 더 이상 동작하지 않는다(이미 클리어된 상태라
  `forceClear()`가 조용히 무시됨). 실사용자 경로에는 영향 없음, 다음에 손볼 수 있음.

---

## DEC-014 — 확장 시스템(패링·아티팩트·NPC 상점·NPC 이벤트·미로 생성) 범위 승인

- 날짜: 2026-08-08
- 상태: 확정 (사용자 명시 요청, AskUserQuestion으로 순서 확인)

### 결정

`MVP_PLAN.md` 범위 확대 금지 항목(영구 성장·상점 등)에 해당하는 다음 5개 시스템을
사용자 승인 하에 순차 진행한다.

1. 패링 시스템(S키, 퍼펙트 시 딜 반사) — 즉시 착수
2. 아티팩트 시스템(공격/유틸/체력 3계열 스탯트리 + 속성 부가 효과)
3. 그림자 조각(처치 보상 재화) + 마을 NPC 강화 상점(탄창·쿨타임·스킬 해금)
4. NPC 이벤트(그림자 실루엣, 우호=유품 텍스트 / 적대=웨이브 트리거)
5. 절차적 미로형 레벨 생성 — 기존 단일 프리셋 랜덤 지형(`rollLayout`) 확장이 아니라
   완전히 새로운 방-연결 생성기로 만든다

### 이유

- 사용자가 한 메시지에서 5개 대형 시스템을 한꺼번에 요청했고, 그중 다수가
  `CLAUDE.md` 4번 규칙(영구 성장·상점·복잡한 환경 변화는 임의 결정 금지)에 해당해
  구현 전 확인이 필요했다.
- AskUserQuestion으로 세 가지를 확인: (1) 패링은 바로 넣는다, (2) 순서는
  아티팩트 → NPC 강화상 → NPC 이벤트, (3) 미로는 기존 시스템 확장이 아니라 새 생성기.

### 검토한 대안

- 전부 한 번에 병렬 착수 — 파일 소유권이 겹치고(예: `Player.ts`를 패링과 아티팩트가
  동시에 건드림) 통합 비용이 커서 기각. 순차 진행으로 결정.

### 영향 범위

- `MVP_PLAN.md`의 "범위 확대 금지" 조항에 대한 예외로 기록. 이후 상점·영구 성장
  관련 코드가 추가돼도 이 결정을 근거로 삼는다.
- `docs/Plan.md`에 P-025~P-029로 반영.

---

## DEC-015 — 강화를 3회 지급한다(방 1·방 2·방 3 후)

> 번호 안내: main에 병합된 팀원 커밋이 이미 DEC-013을 다른 결정(방 1 무전투화)에
> 썼다. 같은 날짜에 독립적으로 번호가 겹쳐 병합 시점에 DEC-015로 다시 매겼다.

- 날짜: 2026-08-08
- 상태: ACCEPTED
- 관련 질문: OQ-016 (RESOLVED)

### 결정

강화 지급 횟수를 2회에서 **3회**로 늘린다. 방 1·방 2 클리어 후는 기존과 같고,
방 3(카운터 방) 클리어 후 역기만 판정 결과를 닫으면 보스 진입 직전에 세 번째 강화를 지급한다.

### 이유

역기만 시연(원거리 위주로 방 1·2를 치르고 방 3에서 근거리로 전환)을 하려면 그 직전에
근거리 강화를 확보할 기회가 있어야 설득력이 생긴다. 2회 지급으로는 방 3 진입 전에
스타일 전환에 쓸 강화를 추가로 얻을 기회가 없었다.

### 검토한 대안

- 2회 유지 — DEC-012에서 이미 보류했던 안. 강화 후보가 8종으로 늘어 특정 강화를
  만날 확률이 낮아진 상황과 맞물려 역기만 시연이 더 어려워지는 쪽으로 남는다.

### 영향 범위

- `src/game/scenes/CombatScene.ts` — `offerUpgrade`가 다음 단계를 결정하는 콜백과
  `final` 플래그를 받도록 바뀜. `resolveDeception`이 `ui:continue` 이후 보스로 바로
  가지 않고 `offerUpgrade(() => this.scene.start("Boss"), true)`를 거친다.
- `src/game/EventBus.ts` — `upgrade:offer` 페이로드에 `final?: boolean` 추가.
- `src/components/HUDOverlay.tsx` — `final`이 참이면 강화 선택 시 방 전환 로딩
  오버레이(`roomLoading`)를 띄우지 않는다. 보스전은 `scene.restart`가 아니라
  `scene.start`라 `room:start`가 발생하지 않아, 기존처럼 로딩을 걸면 걷히지 않고
  멈춘 채로 남기 때문이다.
- `src/game/systems/UpgradeSystem.ts` — 주석만 갱신. 지급 횟수는 호출부(`CombatScene`)
  책임이라 `rollUpgradeChoices` 자체는 손대지 않았다.
- 강화 후보 중복 제외 규칙(OQ-017)은 이번 결정과 무관하게 아직 미결정이다.

---

## DEC-016 — 방 2 구성이 방 1 분석 결과를 축소판 카운터로 반영한다

> 번호 안내: main에 병합된 팀원 커밋이 이미 DEC-014를 다른 결정(패링·아티팩트 등
> 확장 시스템 범위 승인)에 썼다. 병합 시점에 DEC-016으로 다시 매겼다. 아래 결정
> 자체도 병합 이후 전제가 바뀌었다 — 맨 아래 "병합 후 갱신" 참고.

- 날짜: 2026-08-08
- 상태: ACCEPTED
- 관련 질문: OQ-010 (RESOLVED), OQ-009 (방 1 구성은 계속 OPEN)

### 결정

방 2를 방 1 텔레메트리만으로 분류한 스타일에 따라 **축소판(2기) 소프트 카운터**로
바꾼다. 방 3용 카운터 조합(§5)에서 다수 그룹(2기)만 남기고 소수 그룹(1기)은 뺀다.

```text
RANGED 감지 → 추격형 2 (견제형 제외) — room_2_soft_ranged
MELEE  감지 → 견제형 2 (추격형 제외) — room_2_soft_melee
MOBILE 감지 → 기동 카운터형 2 (추격형 제외, 지연 장판 유지) — room_2_soft_mobile
MIXED  감지 → 기존 중립 방(room_2) 그대로 유지
```

### 이유

방 2를 고정 구성으로 두면 런당 "분석→카운터" 체감이 방 3 한 번뿐이라 5~8분 안에
인지되기 어렵다는 우려가 Plan.md에 이미 있었다. 세 가지 반영 방식(스폰 순서 조정만 /
적 1종 교체 / 축소판 카운터방)을 놓고 사용자가 가장 체감이 확실한 축소판을 선택했다.

방 2 텔레메트리가 방 3 분석의 35% 가중치를 그대로 차지하므로(§4), 축소판이라도
방 2를 과도하게 특정 스타일로 몰면 방 1 분석이 방 3 분석에 되먹임되는 위험이 있다.
다수 그룹만 남기고 소수 그룹은 완전히 빼는 대신, 방 3 하드 카운터(3기)보다 적은
2기로 제한해 그 위험을 낮췄다. MIXED(신뢰할 신호 없음)일 때는 아예 손대지 않는다.

### 검토한 대안

- 스폰 순서·타이밍만 조정(적 종류·수 유지) — 피드백 루프 위험이 가장 낮지만
  플레이어가 "방 2가 방 1을 반영했다"를 체감하기 어렵다고 판단해 채택하지 않음.
- 적 1종만 교체 — 중간 강도. 축소판 대비 구현·검증 이점이 크지 않아 채택하지 않음.

### 영향 범위

- `MVP_PLAN.md` §5에 "방 2 소프트 카운터" 절 추가.
- `src/game/data/rooms.ts` — `room_2_soft_ranged`·`room_2_soft_melee`·`room_2_soft_mobile`
  3개 프리셋 추가. 기존 `room_2`는 MIXED·미분석용 중립 방으로 의미가 좁혀졌다.
- `src/game/data/directorRules.ts` — `SOFT_COUNTER_ROOM_2_BY_STYLE` 매핑 추가.
- `src/game/scenes/CombatScene.ts` — `goToNextRoom`이 `resolveRoomId(nextIndex)`로
  분리되고, 방 2 진입 시 `runState.predictedStyle`(방 1 클리어 직후 `analyze()`가
  세팅한, 방 1 단독 분류 값)로 프리셋을 고른다.

### 검증 (병합 전, 방 1이 아직 전투방이던 시점)

Playwright 헤드리스로 실제 원거리 전투를 재현: 모드 전환(K) → 원거리 사격(J)으로
방 1 텔레메트리를 원거리 100%로 만든 뒤 분석 팝업에서 "거리를 두는 자"(RANGED) 확정을
확인 → 강화 선택 후 방 2가 "남은 적 2"(축소판)로 시작하고 스폰된 두 적이 동일한
추격형 실루엣임을 스크린샷으로 확인. MELEE·MOBILE 분기는 동일한 `resolveRoomId` 함수와
매핑 테이블을 타므로 문자열 id 일치를 코드 대조로 확인했다(빌드·타입체크 통과 포함).

### 병합 후 갱신 (2026-08-09)

팀원의 방 1 전면 개편(main의 DEC-013)이 방 1을 무전투 튜토리얼로 바꾸면서, 위
"방 1 텔레메트리로 분류" 전제가 사라졌다. 사용자와 다시 협의해 **방 2 자신의
1웨이브 텔레메트리로 2·3웨이브 구성을 정하는 방식**(방 2도 팀원 쪽 변경으로 이미
3웨이브가 됨)으로 조정했다. 개념(축소판 소프트 카운터, MIXED는 손대지 않음)은
그대로고 "무엇의 텔레메트리를 보는가"만 방 1 → 방 2 1웨이브로 바뀌었다.

- `src/game/scenes/CombatScene.ts` — `resolveRoomId`를 제거하고 `goToNextRoom`을
  원래(고정 순서)대로 되돌렸다. 대신 `resolveWaveOverride(telemetrySoFar, waveIndex)`를
  신설해 방 2 2웨이브 진입 직전에 1웨이브 텔레메트리로 분류, `RoomController`에 주입한다.
- `src/game/systems/RoomController.ts` — `resolveWaveOverride` 훅 신설. 웨이브
  전환 시 이 훅이 돌려준 프리셋을 캐시해 다음 웨이브(들)에도 재사용한다.
- `src/components/ui/AnalysisPanel.tsx`, `src/components/HUDOverlay.tsx` — 방1/방2
  예고 문구를 강약으로 나눴던 `counterStrength` 분기를 되돌렸다. 분석 팝업이 이제
  방 2 클리어 후 한 번만 뜨는 게 유일한 지점이라("hard" 고정) 분기 자체가 죽은
  코드가 됐기 때문이다. `COUNTER_DIALOGUE_SOFT`/`COUNTER_SUMMARY_SOFT`도 함께 제거.
- 검증: 헤드리스로 튜토리얼 게이트 → 방 2 진입까지 실제로 관통했고, 보스는
  `window.runState.setBossWeights`로 SLAM 100%를 강제해 45초간 낙사 미재현을
  확인했다. 방 2의 실제 웨이브 전환(MELEE·MOBILE 포함)은 팀원 쪽 새 전투
  난이도(4기 웨이브, 부활 루프)에서 자동화가 계속 사망해 재확인하지 못했다 —
  사람 실플레이로 남겨둔다(Plan.md P-034 참고).

---

## DEC-017 — 방 단위 함정 플래그(`hazardsEnabled`)를 제거한다

- 날짜: 2026-08-09
- 상태: ACCEPTED
- 관련 질문: OQ-032

### 결정

`RoomPreset.hazardsEnabled`와 `RoomControllerDeps.enableHazards`, `CombatScene.enableHazards`를
전부 걷어낸다. 장판은 기동 카운터형(`MobilityCounterEnemy`)이 스스로 까는 것만 남긴다.

### 이유

플래그는 있었지만 `enableHazards`가 빈 스텁이라 어떤 방에서도 아무 일이 일어나지 않았다.
`counter_mobile`과 `room_2_soft_mobile`이 `hazardsEnabled: true`를 선언해 두고도 실제로는
함정이 없었으니, 계약만 있고 구현이 없는 상태가 "구현된 척"으로 읽혔다.
게다가 `RoomController.start()`는 최초 프리셋의 플래그만 읽어서, 웨이브 오버라이드
(DEC-016)로 바뀐 2·3웨이브의 함정은 구현하더라도 켜지지 않는 구조였다.

3일 차 이후 신규 핵심 기능을 추가하지 않는다는 규칙(CLAUDE.md 5)에 따라, 지금 방 단위
함정을 새로 만들지 않고 죽은 계약을 지우는 쪽을 택했다.

### 대안

- **지연 폭발 장판을 실제로 구현한다** — MVP_PLAN §2를 충족하지만 신규 기능이라 범위가
  커지고 밸런스 재검증이 필요하다. 사용자 판단으로 제외했다.
- **플래그를 남기고 배선만 고친다** — 구현이 없는 한 상태가 그대로다.

### 영향

- `src/game/types/game.ts` — `RoomPreset.hazardsEnabled` 삭제
- `src/game/data/rooms.ts` — 9개 프리셋에서 플래그 삭제, 함정 관련 주석 정리
- `src/game/systems/RoomController.ts` — `enableHazards` 의존성과 호출 삭제
- `src/game/scenes/CombatScene.ts` — `enableHazards` 스텁과 배선 삭제
- `MVP_PLAN.md` §2 방 변수 목록과 §5 MOBILE·MIXED 구성에서 함정 항목 삭제
- 다시 넣으려면 계약(`RoomPreset` 필드)부터 되살려야 한다

---

## DEC-018 — 디버그 방 스킵(F2)을 `?debug=1`로 가둔다

- 날짜: 2026-08-09
- 상태: ACCEPTED

### 결정

`CombatScene`·`BossScene`의 F2(방·보스 즉시 클리어) 키 등록을 `debugFlag("debug")`가
참일 때만 붙인다. `?boss=1`(보스전 직행)은 지금처럼 배포본에도 남긴다.

### 이유

F2에는 아무 가드가 없어 배포본에서도 한 번 누르면 방이 통째로 넘어갔다. 심사 중
오조작 한 번이 런을 무의미하게 만든다. 반면 `?boss=1`은 주소를 알아야만 켜지고
심사·시연에서 보스전만 바로 보여줘야 할 이유가 분명해 그대로 둔다.

`NODE_ENV`로 완전히 제거하는 안은 배포 URL로 시연할 길이 막혀서 택하지 않았다.

### 영향

- `src/game/scenes/CombatScene.ts`, `src/game/scenes/BossScene.ts`
- 시연 시 `?debug=1`을 붙여야 F2가 동작한다
- 같은 작업에서 F2 스킵이 남은 적을 정리하도록 고쳤다 — 이전에는 클리어된 방에서
  적만 계속 공격했다

---

## DEC-019 — 보스 패턴 가중치를 방 3 65% + 방 2 35%로 산출하고, 보스전 성향을 기록에 남긴다

- 날짜: 2026-08-09
- 상태: ACCEPTED
- 관련 질문: OQ-014, OQ-015, OQ-019

### 결정

1. 보스 패턴 가중치의 입력을 방 3 단독에서 **방 3 65% + 방 2 35%** 가중 평균으로 넓힌다.
   방 사이 분석에 이미 쓰는 `RECENCY_WEIGHT`(`analyze`/`mergeRecent`)를 그대로 재사용하고
   새 상수를 만들지 않는다.
2. **역기만 판정(`deception.actualStyle`)은 방 3 단독을 유지한다.**
3. 보스전 텔레메트리로 성향을 계산해 `RunResult.bossStyle`에 남기고 결과 화면에 `보스` 줄로
   보여준다. **표시 전용**이며 보스전 중 가중치를 다시 계산하지 않는다.
4. 결과 화면 맨 위 줄의 이름을 `마지막 전투 방식` → `기록자가 읽은 성향`으로 바꾼다.

### 이유

- 사용자 요청: "보스전할 때도 전 방들의 결과를 바탕으로 보스 패턴을 수정하고 싶다".
- 가중치와 역기만 판정을 갈라 놓아야 둘 다 성립한다. 하나로 합치면, 넓게 보면 스타일 전환의
  보상이 사라지고 좁게 보면 "전 방들을 본다"가 거짓이 된다. 값을 두 개로 나눈 이유가 이것이다.
- 보스전 성향을 가중치에 되먹이지 않는 이유는 단순하다 — 보스가 죽으면 런이 끝나 Director가
  반응할 다음 방이 없다. 되먹이면 MVP_PLAN §8의 "전투 중 재분석하지 않는다"도 깨진다.
- 맨 위 줄은 이제 마지막 전투(=보스)가 아니라 "그 보스가 왜 그렇게 생겼는지"를 말한다.
  이름을 그대로 두면 바로 아래 `보스` 줄과 값이 달라 읽는 사람이 둘 중 무엇을 믿을지 모른다.

### 대안

- **런 전체 누적** — 방이 둘뿐이라 65/35와 실질적으로 같고, 방이 늘어나면 초반 방이 과대
  반영된다. 최근 가중이 Director 설계 의도에 맞다.
- **방 3 단독 유지(현행)** — 사용자 요청과 어긋난다.
- **보스전 성향을 가중치에 되먹임** — 위 이유로 제외. 별도 결정이 필요한 기획 변경이다.

### 영향

- `src/game/types/game.ts` — `RunResult.bossStyle` 추가, `finalStyle` 의미를 주석으로 못 박음
- `src/game/systems/RunState.ts` — `bossWeightStyle`·`bossStyle` 필드와 세터, `reset`/`respawnAtTutorial` 초기화
- `src/game/scenes/CombatScene.ts` — `resolveDeception`이 두 성향을 따로 계산
- `src/game/scenes/BossScene.ts` — 버려지던 보스전 텔레메트리를 성향으로 기록
- `src/components/ui/ResultPanel.tsx` — `보스` 줄 추가, 맨 위 줄 이름 변경, 패턴 섹션명 정리
- **대가**: 방 3에서 스타일을 바꿔도 보스에는 65%만 반영된다. 완료 체크리스트의
  "스타일 전환으로 예측을 빗나가게 할 수 있다"가 그만큼 약해진다. 실플레이에서 체감이
  부족하면 방 3 비중을 `RECENCY_WEIGHT`와 분리해 더 높이는 것을 먼저 검토한다.

---

## 새 결정 템플릿

```md
## DEC-XXX — 결정 제목

- 날짜: YYYY-MM-DD
- 상태: PROPOSED | ACCEPTED | SUPERSEDED
- 관련 질문: OQ-XXX

### 결정
무엇을 결정했는지

### 이유
기간, 기술 위험, 게임성, 배포 안정성 등의 근거

### 대안
검토했지만 선택하지 않은 선택지

### 영향
변경되는 코드, 문서, 일정, 검증 항목
```
